{
  "name": "flippersai-lite",
  "private": true,
  "version": "2.0.0-lite.1",
  "scripts": {
    "build": "node --check lite.js && rm -rf public && mkdir -p public && cp index.html lite.js lite.css brand-app-icon.svg public/"
  }
}
