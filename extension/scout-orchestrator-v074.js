import { api } from './api.js'

const $=(s,r=document)=>r.querySelector(s)
const $$=(s,r=document)=>[...r.querySelectorAll(s)]
const ACTIVE_KEY='flippers_active_scout_session_v068'
const PAUSE_KEY='flippers_scout_paused_v071'
const AUTO_NEW_KEY='flippers_start_new_scan_v074'
const MAX_BATCH=5
const O={generation:1,paused:false,busy:false,engineLoaded:false,oldPaused:true,total:0,done:0,working:0,timer:0,contextTimer:0}

function toast(message){$('.toast')?.remove();const el=document.createElement('div');el.className='toast';el.textContent=message;document.body.appendChild(el);setTimeout(()=>el.remove(),2600)}
const rated=c=>['rated','analysed'].includes(c?.scan_status)||Boolean(c?.recommendation||c?.analysis?.recommendation)
const failed=c=>c?.scan_status==='failed'
const pending=c=>!rated(c)&&!failed(c)
const score=c=>Number(c?.analysis?.overall_score??c?.score??0)
const rec=c=>c?.recommendation||c?.analysis?.recommendation||''
const currentRound=rows=>Math.max(1,...rows.map(c=>Number(c.raw_capture?.round_index||1)))
const roundRows=rows=>{const r=currentRound(rows);return rows.filter(c=>Number(c.raw_capture?.round_index||1)===r)}
const isSupported=url=>/https?:\/\/(?:www\.)?(?:facebook\.com|m\.facebook\.com|ebay\.com\.au|www\.ebay\.com\.au|gumtree\.com\.au|www\.gumtree\.com\.au|depop\.com|www\.depop\.com)\//i.test(String(url||''))
const isSingle=url=>/\/marketplace\/item\/\d+|\/itm\/(?:[^/]+\/)?\d+|\/s-ad\/[^/]+\/[^/]+\/\d+|\/products\/[^/?#]+/i.test(String(url||''))
function normaliseUrl(value=''){try{const u=new URL(value);u.hash='';u.hostname=u.hostname.toLowerCase().replace(/^www\./,'');const drop=['ref','referral_code','__tn__','mibextid','tracking','utm_source','utm_medium','utm_campaign','utm_content'];drop.forEach(k=>u.searchParams.delete(k));u.searchParams.sort();u.pathname=u.pathname.replace(/\/+$/,'')||'/';return`${u.hostname}${u.pathname}${u.search}`}catch{return String(value||'').trim()}}
function rank(a={}){const bonus=a.recommendation==='strong_buy'?20:a.recommendation==='buy'?16:a.recommendation==='negotiate'?8:a.recommendation==='verify_first'?0:-25;return Number(a.overall_score||0)+bonus+Number(a.success_potential||0)/10+Math.max(-10,Math.min(15,Number(a.expected_profit||0)/20))}

async function scanContext(){const stored=(await chrome.storage.local.get(ACTIVE_KEY).catch(()=>({})))[ACTIVE_KEY]||null;let active=[];try{active=await chrome.tabs.query({active:true})}catch{}const supported=active.filter(t=>isSupported(t.url)).sort((a,b)=>Number(b.lastAccessed||0)-Number(a.lastAccessed||0));let tab=supported[0]||null;if(!tab&&active.length&&active.every(t=>String(t.url||'').startsWith('chrome-extension://'))&&stored?.tabId){try{const fallback=await chrome.tabs.get(stored.tabId);if(isSupported(fallback?.url))tab=fallback}catch{}}
  const source=stored?.pageUrl||'';const same=Boolean(tab&&source&&normaliseUrl(tab.url)===normaliseUrl(source));const newCollection=Boolean(tab&&!same&&!isSingle(tab.url));return{stored,tab,same,newCollection}}

function removeDuplicateControls(){$('#scoutRescan')?.remove();$$('.v071-scan-controls').forEach(el=>el.remove())}
function ensureUi(){const head=$('.scout-page-head');if(!head)return false;removeDuplicateControls();let bar=$('#v074RunBar');if(!bar){bar=document.createElement('section');bar.id='v074RunBar';bar.className='v074-run-controls';bar.innerHTML='<div class="v074-run-copy"><span>SCAN CONTROLS</span><strong>Scout screens 5 listings at a time.</strong></div><div class="v074-run-actions"><button class="button secondary small" id="v074Pause">Stop scan</button><button class="button soft small" id="v074Restart">Restart current scan</button><button class="button soft small" id="v074New">Start new scan</button></div>';const source=$('.scout-capture-source');(source||head).insertAdjacentElement('afterend',bar)}let loader=$('#v074Loading');if(!loader){loader=document.createElement('section');loader.id='v074Loading';loader.className='scout-loading-indicator visible';loader.setAttribute('role','status');loader.setAttribute('aria-live','polite');loader.innerHTML='<div class="scout-loading-spinner" aria-hidden="true"></div><div class="scout-loading-copy"><strong>FlippersAI Scout is working…</strong><span>Screening the next 5 listings.</span></div><div class="scout-loading-count">0/0</div><div class="scout-loading-track"><i style="width:4%"></i></div>';bar.insertAdjacentElement('afterend',loader)}syncPauseLabel();refreshControlState();return true}
function syncPauseLabel(){const b=$('#v074Pause');if(b)b.textContent=O.paused?'Resume scan':'Stop scan'}
async function refreshControlState(){const restart=$('#v074Restart'),fresh=$('#v074New');if(!restart&&!fresh)return;const ctx=await scanContext();if(restart){restart.disabled=!ctx.same;restart.title=ctx.same?'Rescan the current Marketplace page/listings':'Return to the page this Scout belongs to before restarting it.'}if(fresh){fresh.disabled=!ctx.newCollection;fresh.title=ctx.newCollection?'Start a Scout on the new Marketplace results page':'Open a different Marketplace results page to start a new Scout.'}}
function summaryNumber(label){const cell=$$('.scout-summary>div').find(x=>x.querySelector('span')?.textContent?.trim().toUpperCase()===label);return Number(cell?.querySelector('strong')?.textContent||0)}
function updateLoader(message=''){const el=$('#v074Loading');if(!el)return;const total=O.total||summaryNumber('THIS ROUND')||summaryNumber('SCREENING'),screened=Math.max(O.done,summaryNumber('SCREENED')||summaryNumber('RATED')),working=Math.max(O.working,summaryNumber('WORKING')),insight=$('.scout-insight'),verifying=/verif/i.test(insight?.textContent||'')&&insight?.classList.contains('scanning'),active=!O.paused&&(O.busy||working>0||verifying||(total>0&&screened<total));const strong=$('.scout-loading-copy strong',el),copy=$('.scout-loading-copy span',el),count=$('.scout-loading-count',el),track=$('.scout-loading-track i',el);if(O.paused){el.classList.add('visible','paused');if(strong)strong.textContent='Scout paused';if(copy)copy.textContent=`${screened}/${total} screened. Progress is saved.`}else if(active){el.classList.add('visible');el.classList.remove('paused');if(strong)strong.textContent='FlippersAI Scout is working…';if(copy)copy.textContent=message||`${screened}/${total} screened${working?` · ${working} working now`:''}${verifying?' · verifying promising listings':''}.`;if(track)track.style.width=`${Math.max(4,Math.min(98,Math.round(screened/Math.max(1,total)*100)))}%`}else{el.classList.remove('visible','paused');if(track)track.style.width='100%'}if(count)count.textContent=`${screened}/${total}`}

async function loadRows(){const ids=$$('.scout-candidate[data-candidate]').map(x=>x.dataset.candidate).filter(Boolean);if(!ids.length)return[];return api.select('scout_candidates',`select=*&id=in.(${ids.join(',')})`).catch(()=>[])}
async function saveResult(c,r,engine,generation){if(generation!==O.generation||O.paused)return;const a={...r,engine_version:engine||'flippers-scout-batch-5',scout_scan_depth:'search_page',scout_enriched:false},body={analysis:a,scan_status:'rated',recommendation:a.recommendation||null,score:a.overall_score??null,resale_mid:a.resale_mid??null,expected_profit:a.expected_profit??null,expected_roi_percent:a.expected_roi_percent??null,rank_score:rank(a),selected:false,updated_at:new Date().toISOString()};await api.update('scout_candidates',`id=eq.${c.id}`,body)}
async function markFailed(c,error,generation){if(generation!==O.generation||O.paused)return;await api.update('scout_candidates',`id=eq.${c.id}`,{scan_status:'failed',analysis:{error:String(error||'Scout screening failed')},updated_at:new Date().toISOString()}).catch(()=>{})}
async function screenChunk(chunk,generation){O.working=chunk.length;updateLoader(`Screening ${chunk.length} listing${chunk.length===1?'':'s'} now…`);try{const items=chunk.map(c=>({id:String(c.id),title:c.title||'',asking_price:c.asking_price,location:c.location||'',category:c.category_label||c.raw_capture?.category_label||'Other',visible_text:String(c.raw_capture?.raw_text||'').slice(0,1800),thumbnail:null})),data=await api.invoke('scout-batch-screen',{items});if(generation!==O.generation||O.paused)return;if(data?.error)throw new Error(data.error);const map=new Map((data.results||[]).map(r=>[String(r.id),r]));await Promise.all(chunk.map(c=>{const r=map.get(String(c.id));return r?saveResult(c,r,data.engine_version,generation):markFailed(c,'No batch result returned',generation)}))}catch(error){if(generation===O.generation&&!O.paused)await Promise.all(chunk.map(c=>markFailed(c,error.message||error,generation)))}finally{if(generation===O.generation)O.working=0}}
async function syncMarketplaceRatings(){const rows=await loadRows(),ratings=rows.filter(rated).map(c=>{const a=c.analysis||{};return{id:String(c.id),listingId:c.listing_id||'',url:c.source_url||'',score:Math.round(score(c)),recommendation:rec(c),profit:a.expected_profit??null,resale:a.resale_mid??null,roi:a.expected_roi_percent??null,successPotential:a.success_potential??null,authenticityStatus:a.authenticity_status||'',scannedAt:c.updated_at||new Date().toISOString()}});await chrome.runtime.sendMessage({type:'FLIPPERS_ROUTE_RATING_OVERLAY',enabled:true,ratings}).catch(()=>{})}

function clickOldPauseControl(){let b=$('#v071Pause'),temporary=false;if(!b){b=document.createElement('button');b.id='v071Pause';b.type='button';b.hidden=true;document.body.appendChild(b);temporary=true}b.click();if(temporary)b.remove()}
async function pauseOldEngine(){if(!O.engineLoaded||O.oldPaused)return;clickOldPauseControl();O.oldPaused=true;await new Promise(r=>setTimeout(r,25))}
async function resumeOldEngine(){if(!O.engineLoaded||!O.oldPaused||O.paused)return;clickOldPauseControl();O.oldPaused=false;await new Promise(r=>setTimeout(r,25))}
async function ensureEngine(){if(O.engineLoaded)return;await chrome.storage.local.set({[PAUSE_KEY]:true});await import('./scout-engine-v071.js');O.engineLoaded=true;O.oldPaused=true;await new Promise(r=>setTimeout(r,100));removeDuplicateControls()}

async function fastScreen(){if(O.busy||O.paused||!$('.scout-list'))return;O.busy=true;const generation=O.generation;try{await pauseOldEngine();let rows=await loadRows(),rr=roundRows(rows),waiting=rr.filter(pending);O.total=rr.length;O.done=rr.length-waiting.length;updateLoader();while(waiting.length&&generation===O.generation&&!O.paused){const chunk=waiting.slice(0,MAX_BATCH);await screenChunk(chunk,generation);if(generation!==O.generation||O.paused)return;rows=await loadRows();rr=roundRows(rows);waiting=rr.filter(pending);O.done=rr.length-waiting.length;await syncMarketplaceRatings();document.dispatchEvent(new CustomEvent('flippers:candidate-updated'));updateLoader()}if(generation!==O.generation||O.paused)return;await ensureEngine();document.dispatchEvent(new CustomEvent('flippers:candidate-updated'));await new Promise(r=>setTimeout(r,100));await resumeOldEngine()}finally{if(generation===O.generation){O.busy=false;O.working=0;updateLoader();refreshControlState()}}}

async function stopScan(){if(O.paused)return;O.paused=true;O.generation+=1;O.busy=false;O.working=0;await chrome.storage.local.set({[PAUSE_KEY]:true});await pauseOldEngine();document.dispatchEvent(new CustomEvent('flippers:scout-hard-stop'));syncPauseLabel();updateLoader();toast('Scout stopped immediately. Completed progress is saved.')}
async function resumeScan(){if(!O.paused)return;O.paused=false;await chrome.storage.local.set({[PAUSE_KEY]:true});syncPauseLabel();updateLoader('Resuming with the next 5 listings…');await fastScreen();if(!O.paused)await resumeOldEngine()}
async function restartRound(){const ctx=await scanContext();if(!ctx.same)return toast('Restart is only available while you are still on the page this Scout belongs to.');O.paused=true;O.generation+=1;O.busy=false;O.working=0;await chrome.storage.local.set({[PAUSE_KEY]:true});await pauseOldEngine();document.dispatchEvent(new CustomEvent('flippers:scout-hard-stop'));const rows=await loadRows(),rr=roundRows(rows),reset={scan_status:'quick',analysis:{},recommendation:null,score:null,resale_mid:null,expected_profit:null,expected_roi_percent:null,rank_score:null,selected:false,deep_capture:{},updated_at:new Date().toISOString()};await Promise.all(rr.map(c=>api.update('scout_candidates',`id=eq.${c.id}`,reset).catch(()=>null)));O.paused=false;O.total=rr.length;O.done=0;syncPauseLabel();document.dispatchEvent(new CustomEvent('flippers:candidate-updated'));updateLoader('Restarting this page from the first 5 listings…');toast('Current Scout restarted');await fastScreen()}
async function startNewScan(){const ctx=await scanContext();if(!ctx.newCollection)return toast('Open a different Marketplace results page first.');O.paused=true;O.generation+=1;O.busy=false;O.working=0;await chrome.storage.local.set({[PAUSE_KEY]:true,[AUTO_NEW_KEY]:{requestedAt:Date.now(),target:normaliseUrl(ctx.tab.url)}});await pauseOldEngine();document.dispatchEvent(new CustomEvent('flippers:scout-hard-stop'));await chrome.storage.local.remove(ACTIVE_KEY);location.reload()}
async function autoStartNew(){const saved=(await chrome.storage.local.get(AUTO_NEW_KEY).catch(()=>({})))[AUTO_NEW_KEY];if(!saved)return;for(let i=0;i<70;i++){const button=$('#scanCurrent');if(button){await chrome.storage.local.remove([AUTO_NEW_KEY,PAUSE_KEY]);button.click();return}await new Promise(r=>setTimeout(r,100))}await chrome.storage.local.remove(AUTO_NEW_KEY);toast('Could not start the new Scout automatically. Press Scan current page once.')}

function scheduleStatus(){clearTimeout(O.timer);O.timer=setTimeout(()=>{ensureUi();updateLoader();refreshControlState()},100)}
document.addEventListener('click',e=>{if(e.target.closest?.('#v074Pause')){e.preventDefault();e.stopImmediatePropagation();(O.paused?resumeScan():stopScan()).catch(err=>toast(err.message));return}if(e.target.closest?.('#v074Restart')){e.preventDefault();e.stopImmediatePropagation();restartRound().catch(err=>toast(err.message));return}if(e.target.closest?.('#v074New')){e.preventDefault();e.stopImmediatePropagation();startNewScan().catch(err=>toast(err.message));return}},true)
document.addEventListener('flippers:scout-rendered',()=>{ensureUi();pauseOldEngine().finally(()=>fastScreen().catch(err=>toast(err.message)))})
document.addEventListener('flippers:candidate-updated',scheduleStatus)
new MutationObserver(ms=>{if(ms.every(m=>m.target.closest?.('#v074RunBar,#v074Loading')))return;scheduleStatus()}).observe(document.getElementById('app'),{childList:true,subtree:true,attributes:true,attributeFilter:['class']})

;(async()=>{const stored=await chrome.storage.local.get(PAUSE_KEY).catch(()=>({}));O.paused=Boolean(stored[PAUSE_KEY]);ensureUi();clearInterval(O.contextTimer);O.contextTimer=setInterval(()=>{removeDuplicateControls();refreshControlState().catch(()=>{})},700);autoStartNew().catch(()=>{});if($('.scout-list')){if(O.paused){syncPauseLabel();updateLoader();return}await chrome.storage.local.set({[PAUSE_KEY]:true});syncPauseLabel();fastScreen().catch(err=>toast(err.message))}})()
