import { api } from './api.js'

const $=(s,r=document)=>r.querySelector(s)
const $$=(s,r=document)=>[...r.querySelectorAll(s)]
const sleep=ms=>new Promise(r=>setTimeout(r,ms))
const ACTIVE_KEY='flippers_active_scout_session_v068'
const USER_PAUSE_KEY='flippers_scout_user_paused_v076'
const STOP_KEY='flippers_scout_stopped_v076'
const AUTO_NEW_KEY='flippers_start_new_scan_v074'
const MAX_BATCH=5
const SHORTLIST_SCORE=65
const REQUEST_TIMEOUT=15000
const O={generation:1,paused:false,stopped:false,busy:false,total:0,done:0,batchNo:0,batchCount:0,timer:0,contextTimer:0,heartbeat:0,sessionId:null,active:new Map(),batchStartedAt:0,durations:[],enrichQueue:[],enriching:new Set(),enrichWorkers:0,enrichTabs:new Set(),lastError:''}
document.body.dataset.scoutProgressOwner='v080'

function toast(message){$('.toast')?.remove();const el=document.createElement('div');el.className='toast';el.textContent=message;document.body.appendChild(el);setTimeout(()=>el.remove(),2800)}
const rated=c=>['rated','analysed'].includes(c?.scan_status)||Boolean(c?.recommendation||c?.analysis?.recommendation)
const failed=c=>c?.scan_status==='failed'
const pending=c=>!rated(c)&&!failed(c)
const score=c=>Number(c?.analysis?.overall_score??c?.score??0)
const rec=c=>c?.recommendation||c?.analysis?.recommendation||''
const category=c=>c?.category_label||c?.raw_capture?.category_label||c?.analysis?.category||'Other'
const currentRound=rows=>Math.max(1,...rows.map(c=>Number(c.raw_capture?.round_index||1)))
const roundRows=rows=>{if(!rows.length)return[];const r=currentRound(rows);return rows.filter(c=>Number(c.raw_capture?.round_index||1)===r)}
const risky=c=>['likely_counterfeit','high_risk'].includes(c?.analysis?.authenticity_status)
const worthwhile=c=>rated(c)&&!failed(c)&&score(c)>=SHORTLIST_SCORE&&rec(c)!=='skip'&&!risky(c)
const quickWorthwhile=c=>rated(c)&&!failed(c)&&score(c)>=60&&rec(c)!=='skip'
const isSupported=url=>/^https:\/\/([^/]+\.)?(facebook\.com|ebay\.com\.au|gumtree\.com\.au|depop\.com)\//i.test(String(url||''))
const isSingle=url=>/\/marketplace\/item\/\d+|\/itm\/(?:[^/]+\/)?\d+|\/s-ad\/[^/]+\/[^/]+\/\d+|\/products\/[^/?#]+/i.test(String(url||''))
function normaliseUrl(value=''){try{const u=new URL(value);u.hash='';u.hostname=u.hostname.toLowerCase().replace(/^www\./,'');['ref','referral_code','__tn__','mibextid','tracking','utm_source','utm_medium','utm_campaign','utm_content'].forEach(k=>u.searchParams.delete(k));u.searchParams.sort();u.pathname=u.pathname.replace(/\/+$/,'')||'/';return`${u.hostname}${u.pathname}${u.search}`}catch{return String(value||'').trim()}}
function rank(a={}){const bonus=a.recommendation==='strong_buy'?20:a.recommendation==='buy'?16:a.recommendation==='negotiate'?8:a.recommendation==='verify_first'?0:-25;return Number(a.overall_score||0)+bonus+Number(a.success_potential||0)/10+Math.max(-10,Math.min(15,Number(a.expected_profit||0)/20))}
function fmtTime(seconds){if(!Number.isFinite(seconds)||seconds<=0)return'';if(seconds<60)return`~${Math.max(1,Math.round(seconds))} sec`;const m=Math.floor(seconds/60),s=Math.round(seconds%60);return s?`~${m}m ${s}s`:`~${m} min`}

async function activeScout(){return (await chrome.storage.local.get(ACTIVE_KEY).catch(()=>({})))[ACTIVE_KEY]||null}
async function sourceContext(){const stored=await activeScout();let sourceTab=null;if(stored?.tabId){try{const t=await chrome.tabs.get(stored.tabId);if(t?.id&&isSupported(t.url)&&normaliseUrl(t.url)===normaliseUrl(stored.pageUrl||''))sourceTab=t}catch{}}let activeTab=null;try{[activeTab]=await chrome.tabs.query({active:true,currentWindow:true})}catch{}const activeNew=Boolean(activeTab?.id&&isSupported(activeTab.url)&&!isSingle(activeTab.url)&&normaliseUrl(activeTab.url)!==normaliseUrl(stored?.pageUrl||''));return{stored,sourceTab,sourceAlive:Boolean(sourceTab),activeTab,newCollection:activeNew}}
async function loadRows(){const stored=await activeScout();if(stored?.sessionId){const rows=await api.select('scout_candidates',`select=*&session_id=eq.${encodeURIComponent(stored.sessionId)}`).catch(()=>[]);return(rows||[]).sort((a,b)=>Number(a.raw_capture?.order_index??99999)-Number(b.raw_capture?.order_index??99999))}const ids=$$('.scout-candidate[data-candidate]').map(x=>x.dataset.candidate).filter(Boolean);if(!ids.length)return[];return api.select('scout_candidates',`select=*&id=in.(${ids.join(',')})`).catch(()=>[])}

function removeDuplicateControls(){$('#scoutRescan')?.remove();$$('.v071-scan-controls,.v074-run-controls,.v075-run-controls,.v076-run-controls:not(#v080RunBar)').forEach(el=>el.remove());$('#v074Loading')?.remove();$('#v075Loading')?.remove();$('#v076Loading:not(#v080Loading)')?.remove()}
function ensureUi(){const head=$('.scout-page-head');if(!head)return false;removeDuplicateControls();let bar=$('#v080RunBar');if(!bar){bar=document.createElement('section');bar.id='v080RunBar';bar.className='v076-run-controls';bar.innerHTML='<div class="v076-run-copy"><span>SCAN CONTROLS</span><strong>Scout works through one group of 5 at a time and shows every result as it finishes.</strong></div><div class="v076-run-actions"><button class="button danger small" id="v080Stop">Stop scan</button><button class="button secondary small" id="v080Pause">Pause scan</button><button class="button soft small" id="v080Restart">Restart current scan</button><button class="button soft small" id="v080New">Start new scan</button></div>';const source=$('.scout-capture-source');(source||head).insertAdjacentElement('afterend',bar)}let loader=$('#v080Loading');if(!loader){loader=document.createElement('section');loader.id='v080Loading';loader.className='scout-loading-indicator visible';loader.setAttribute('role','status');loader.setAttribute('aria-live','polite');loader.innerHTML='<div class="scout-loading-spinner" aria-hidden="true"></div><div class="scout-loading-copy"><strong>Starting Scout…</strong><span>Preparing the first 5 listings.</span><small class="v080-live-detail"></small></div><div class="scout-loading-count">Batch 1</div><div class="scout-loading-track"><i style="width:0%"></i></div>';bar.insertAdjacentElement('afterend',loader)}syncControlLabels();refreshControlState();return true}
function syncControlLabels(){const pause=$('#v080Pause');if(pause)pause.textContent=O.paused?'Resume scan':'Pause scan';const stop=$('#v080Stop');if(stop)stop.textContent=O.stopped?'Scan stopped':'Stop scan'}
async function refreshControlState(){const stop=$('#v080Stop'),pause=$('#v080Pause'),restart=$('#v080Restart'),fresh=$('#v080New');if(!stop&&!pause&&!restart&&!fresh)return;const ctx=await sourceContext();const rows=roundRows(await loadRows().catch(()=>[]));const remaining=rows.filter(pending).length;if(stop){stop.disabled=O.stopped;stop.title=O.stopped?'This scan has stopped. Restart it or start a new scan.':'Completely stop this Scout.'}if(pause){pause.disabled=O.stopped||(!remaining&&!O.busy&&O.active.size===0&&!O.enrichWorkers);pause.title=O.paused?'Resume from saved progress.':'Temporarily pause this Scout.'}if(restart){restart.disabled=!ctx.sourceAlive;restart.title=ctx.sourceAlive?'Rescan the listings from the original Scout page.':'The original Scout page is no longer open at the same URL.'}if(fresh){fresh.disabled=!ctx.newCollection;fresh.title=ctx.newCollection?'Start Scout on the new marketplace results page.':'Open a different marketplace results page to start a new Scout.'}}

function ensureRemoveButton(c){const el=$(`.scout-candidate[data-candidate="${CSS.escape(String(c.id))}"]`);if(!el)return;let x=$('[data-v076-remove]',el);if(!x){x=document.createElement('button');x.type='button';x.className='scout-dismiss';x.dataset.v076Remove=String(c.id);x.title='Remove this listing from FlippersAI';x.textContent='×';el.appendChild(x)}}
function estimatedSeconds(rr){if(!O.durations.length)return null;const avg=O.durations.slice(-10).reduce((a,b)=>a+b,0)/Math.min(10,O.durations.length);const unresolved=rr.filter(pending).length+O.active.size;return avg*Math.ceil(unresolved/MAX_BATCH)/1000}
function updateLoader(rows=[],message=''){const el=$('#v080Loading');if(!el)return;const rr=roundRows(rows),ratedRows=rr.filter(rated),failedRows=rr.filter(failed),working=O.active.size,total=rr.length,processed=ratedRows.length+failedRows.length,remaining=Math.max(0,total-processed),strong=$('.scout-loading-copy strong',el),copy=$('.scout-loading-copy span',el),detail=$('.v080-live-detail',el),count=$('.scout-loading-count',el),track=$('.scout-loading-track i',el);el.classList.remove('paused','stopped','error');const currentBatch=Math.min(Math.max(1,O.batchNo||Math.floor(processed/MAX_BATCH)+1),Math.max(1,Math.ceil(total/MAX_BATCH)));const batchCount=Math.max(1,Math.ceil(total/MAX_BATCH));if(O.stopped){el.classList.add('visible','stopped');if(strong)strong.textContent='Scout stopped';if(copy)copy.textContent=`${ratedRows.length}/${total} rated. No further scanning will run.`}else if(O.paused){el.classList.add('visible','paused');if(strong)strong.textContent='Scout paused';if(copy)copy.textContent=`${ratedRows.length}/${total} rated. Progress is saved.`}else if(working||remaining){el.classList.add('visible');if(strong)strong.textContent='FlippersAI Scout is working…';if(copy)copy.textContent=message||`${working} working now · ${ratedRows.length}/${total} rated · results appear one-by-one.`;const elapsed=O.batchStartedAt?Date.now()-O.batchStartedAt:0;const estimate=estimatedSeconds(rr);if(detail)detail.textContent=failedRows.length?`${failedRows.length} scan issue${failedRows.length===1?'':'s'} · ${estimate?`${fmtTime(estimate)} remaining`:elapsed>12000?'Taking longer than expected — still working':''}`:estimate?`Estimated ${fmtTime(estimate)} remaining`:elapsed>12000?'Taking longer than expected — still waiting for the first results…':''} }else if(failedRows.length){el.classList.add('visible','error');if(strong)strong.textContent='Scout finished with scan errors';if(copy)copy.textContent=`${ratedRows.length}/${total} rated · ${failedRows.length} could not be rated. Restart current scan to retry them.`;if(detail)detail.textContent='The failed listings were not counted as rated or filtered out.'}else if(O.enrichWorkers||O.enrichQueue.length){el.classList.add('visible');if(strong)strong.textContent='Ratings complete · checking promising leads…';if(copy)copy.textContent=`${ratedRows.length}/${total} rated. Seller, condition, photos and authenticity checks continue in the background.`}else{el.classList.remove('visible');if(detail)detail.textContent=''}if(count)count.textContent=total?`Batch ${currentBatch}/${batchCount}`:'Batch 1';if(track)track.style.width=`${total?Math.round(processed/total*100):0}%`}
function renderLive(rows){const rr=roundRows(rows),ratedRows=rr.filter(rated),failedRows=rr.filter(failed),short=rr.filter(worthwhile),filtered=ratedRows.filter(c=>!worthwhile(c)),working=Math.min(MAX_BATCH,O.active.size);O.total=rr.length;O.done=ratedRows.length;O.batchCount=Math.max(1,Math.ceil(rr.length/MAX_BATCH));$$('.scout-candidate[data-candidate]').forEach(el=>el.classList.remove('v076-shortlist-visible'));short.forEach(c=>{const el=$(`.scout-candidate[data-candidate="${CSS.escape(String(c.id))}"]`);if(!el)return;el.classList.add('v076-shortlist-visible');ensureRemoveButton(c);const pill=$('.scout-rec',el);if(pill&&!c.analysis?.scout_enriched){pill.className='scout-rec warn';pill.textContent='Promising · quick screen'}const note=$('.scout-quick-note',el);if(note&&!c.analysis?.scout_enriched)note.textContent='Worth a look. You can Save, Analyse, Open or remove this while Scout continues.'})
  const box=$('.scout-summary');if(box){const cells=[...box.children];const data=[['FOUND',rr.length],['RATED',ratedRows.length],['WORKING',working],['SHORTLIST',short.length],['FILTERED OUT',filtered.length]];data.forEach(([label,value],i)=>{if(cells[i]){cells[i].innerHTML=`<span>${label}</span><strong>${value}</strong>`;cells[i].title=label==='FOUND'?'Total listings found on this Scout page.':label==='RATED'?'Every listing whose rating has completed, whether shortlisted or filtered out.':label==='WORKING'?'Listings actively being rated right now. Maximum 5.':label==='SHORTLIST'?'Completed ratings good enough to show in the shortlist.':'Completed ratings deliberately kept out of the shortlist.'}})}
  const insight=$('.scout-insight');if(insight){if(O.stopped){insight.classList.remove('scanning');insight.innerHTML=`<strong>Scout stopped</strong><span>${ratedRows.length}/${rr.length} rated. Completed results are saved.</span>`}else if(O.paused){insight.classList.remove('scanning');insight.innerHTML=`<strong>Scout paused</strong><span>${ratedRows.length}/${rr.length} rated. Resume whenever you are ready.</span>`}else if(ratedRows.length+failedRows.length<rr.length||working){insight.classList.add('scanning');insight.innerHTML=`<strong>Live Scout progress</strong><span>${working} working · ${ratedRows.length} rated · ${short.length} shortlisted · ${filtered.length} filtered out${failedRows.length?` · ${failedRows.length} scan issue${failedRows.length===1?'':'s'}`:''}. You can switch browser tabs; Scout keeps running.</span>`}else if(failedRows.length){insight.classList.remove('scanning');insight.innerHTML=`<strong>Scout needs a retry</strong><span>${ratedRows.length}/${rr.length} rated · ${failedRows.length} could not be rated. Restart current scan to retry the page.</span>`}else if(O.enrichWorkers||O.enrichQueue.length){insight.classList.add('scanning');insight.innerHTML=`<strong>Ratings complete · verifying promising leads</strong><span>${short.length} shortlisted. Deeper seller, condition and authenticity checks continue in the background.</span>`}else{insight.classList.remove('scanning');insight.innerHTML=short.length?`<strong>${short.length} worthwhile lead${short.length===1?'':'s'} surfaced</strong><span>${filtered.length} filtered out. Every completed listing has been accounted for.</span>`:`<strong>No worthwhile leads found</strong><span>${filtered.length} listings were rated and filtered out.</span>`}}
  updateLoader(rows);syncControlLabels()}
async function liveSync(){const rows=await loadRows().catch(()=>[]);renderLive(rows);syncMarketplaceRatings(rows).catch(()=>{});document.dispatchEvent(new CustomEvent('flippers:candidate-updated'));return rows}

async function saveResult(c,r,engine,generation){if(generation!==O.generation||O.paused||O.stopped)return;const a={...r,engine_version:engine||'flippers-scout-quick-1',scout_scan_depth:'search_page',scout_enriched:false},body={analysis:a,scan_status:'rated',recommendation:a.recommendation||null,score:a.overall_score??null,resale_mid:a.resale_mid??null,expected_profit:a.expected_profit??null,expected_roi_percent:a.expected_roi_percent??null,rank_score:rank(a),selected:false,updated_at:new Date().toISOString()};await api.update('scout_candidates',`id=eq.${c.id}`,body)}
async function markFailed(c,error,generation){if(generation!==O.generation||O.paused||O.stopped)return;await api.update('scout_candidates',`id=eq.${c.id}`,{scan_status:'failed',analysis:{error:String(error||'Scout screening failed'),scout_scan_depth:'search_page'},updated_at:new Date().toISOString()}).catch(()=>{})}
function withTimeout(promise,ms){return Promise.race([promise,new Promise((_,reject)=>setTimeout(()=>reject(new Error('Scout rating timed out')),ms))])}
async function rateOne(c,generation){const id=String(c.id),started=Date.now();O.active.set(id,{started});await liveSync();let lastError=null;try{for(let attempt=1;attempt<=2;attempt++){if(generation!==O.generation||O.paused||O.stopped)return;try{const item={id,title:c.title||'',asking_price:c.asking_price,location:c.location||'',category:category(c),visible_text:String(c.raw_capture?.raw_text||'').slice(0,2200)},data=await withTimeout(api.invoke('scout-quick-rate',{item}),REQUEST_TIMEOUT);if(data?.error)throw new Error(data.error);if(!data?.result)throw new Error('No quick rating returned');await saveResult(c,data.result,data.engine_version,generation);O.durations.push(Date.now()-started);return}catch(error){lastError=error;if(attempt<2&&generation===O.generation&&!O.paused&&!O.stopped){O.active.set(id,{started,retrying:true});await liveSync();await sleep(650)}}}if(generation===O.generation&&!O.paused&&!O.stopped)await markFailed(c,lastError?.message||lastError,generation)}finally{if(generation===O.generation){O.active.delete(id);await liveSync()}}}

async function syncMarketplaceRatings(rows){const ratings=(rows||await loadRows()).filter(rated).map(c=>{const a=c.analysis||{};return{id:String(c.id),listingId:c.listing_id||'',url:c.source_url||'',score:Math.round(score(c)),recommendation:rec(c),profit:a.expected_profit??null,resale:a.resale_mid??null,roi:a.expected_roi_percent??null,successPotential:a.success_potential??null,authenticityStatus:a.authenticity_status||'',scannedAt:c.updated_at||new Date().toISOString()}});await chrome.runtime.sendMessage({type:'FLIPPERS_ROUTE_RATING_OVERLAY',enabled:true,ratings}).catch(()=>{});const ctx=await sourceContext();if(ctx.sourceTab?.id){await chrome.scripting.executeScript({target:{tabId:ctx.sourceTab.id},files:['scout-rating-overlay.js','marketplace-trust-overlay-v079.js']}).catch(()=>{});await chrome.tabs.sendMessage(ctx.sourceTab.id,{type:'FLIPPERS_RATING_OVERLAY_V067',enabled:true,ratings}).catch(()=>{})}}

async function waitTab(id,timeout=18000){return new Promise((resolve,reject)=>{let done=false;const finish=e=>{if(done)return;done=true;clearTimeout(timer);chrome.tabs.onUpdated.removeListener(listener);e?reject(e):resolve()};const listener=(tabId,info)=>{if(tabId===id&&info.status==='complete')finish()};chrome.tabs.onUpdated.addListener(listener);const timer=setTimeout(()=>finish(new Error('Timed out loading listing')),timeout);chrome.tabs.get(id).then(tab=>{if(tab.status==='complete')finish()}).catch(()=>{})})}
async function scanTab(id){let last;for(let i=0;i<4;i++){try{const r=await chrome.tabs.sendMessage(id,{type:'FLIPPERS_SCAN_PAGE'});if(r?.ok)return r.data;if(r?.error)last=new Error(r.error)}catch(e){last=e;try{await chrome.scripting.executeScript({target:{tabId:id},files:['content-v070.js']})}catch{};await sleep(250+i*180)}}throw last||new Error('Could not read listing page')}
function toDataUrl(buffer,mime='image/jpeg'){const bytes=new Uint8Array(buffer);let binary='';for(let i=0;i<bytes.length;i+=32768)binary+=String.fromCharCode(...bytes.subarray(i,Math.min(i+32768,bytes.length)));return`data:${mime};base64,${btoa(binary)}`}
async function loadImage(url,max=3000000){if(!/^https?:/i.test(String(url||'')))return null;try{const r=await fetch(url,{credentials:'include',cache:'force-cache'});if(!r.ok)return null;const type=r.headers.get('content-type')||'';if(!type.startsWith('image/'))return null;const b=await r.blob();if(b.size>max)return null;return toDataUrl(await b.arrayBuffer(),type)}catch{return null}}
async function captureListing(c,generation){const tab=await chrome.tabs.create({url:c.source_url,active:false});O.enrichTabs.add(tab.id);try{await waitTab(tab.id);if(generation!==O.generation||O.paused||O.stopped)throw new Error('Cancelled');await sleep(400);return await scanTab(tab.id)}finally{O.enrichTabs.delete(tab.id);chrome.tabs.remove(tab.id).catch(()=>{})}}
function applyEnrichment(prior,enrich,capture,c){const a={...prior,...enrich,scout_enriched:true,scout_scan_depth:'enriched'},risk=['likely_counterfeit','high_risk'].includes(a.authenticity_status),uncertain=a.authenticity_status==='uncertain'&&['Sneakers','Audio','Watches','Fashion','Collectibles'].includes(category(c));if(enrich.gate==='skip'||a.authenticity_status==='likely_counterfeit'){a.recommendation='skip';a.overall_score=Math.min(Number(a.overall_score||0),20);a.success_potential=Math.min(Number(a.success_potential||0),15)}else if(enrich.gate==='verify'||risk||uncertain){if(['strong_buy','buy','negotiate'].includes(a.recommendation))a.recommendation='verify_first';a.overall_score=Math.min(Number(a.overall_score||0),60);a.success_potential=Math.min(Number(a.success_potential||0),55)}if(risk||uncertain){a.resale_low=null;a.resale_mid=null;a.resale_high=null;a.expected_profit=null;a.expected_roi_percent=null}else{const ask=Number(capture.askingPrice??c.asking_price),target=Number(enrich.target_resale_mid??a.resale_mid),prep=Math.max(0,Number(enrich.estimated_prep_cost||0)),selling=Math.max(0,Number(a.expected_selling_costs||0));if(Number.isFinite(target)&&target>0){a.resale_mid=target;a.resale_high=target;if(Number.isFinite(ask)){a.expected_profit=+(target-ask-prep-selling).toFixed(2);const invested=ask+prep;a.expected_roi_percent=invested>0?+((a.expected_profit/invested)*100).toFixed(1):null;if(a.expected_profit<=0){a.recommendation='skip';a.overall_score=Math.min(Number(a.overall_score||0),45);a.success_potential=Math.min(Number(a.success_potential||0),35)}}}if(enrich.material_damage){a.overall_score=Math.min(Number(a.overall_score||0),65);a.success_potential=Math.min(Number(a.success_potential||0),60)}}return a}
async function enrichOne(c,generation){if(c.analysis?.scout_enriched||O.paused||O.stopped||generation!==O.generation)return;O.enriching.add(String(c.id));try{const capture=await captureListing(c,generation);if(generation!==O.generation||O.paused||O.stopped)return;const images=[];for(const url of(capture.imageUrls||[]).slice(0,5)){if(generation!==O.generation||O.paused||O.stopped)return;const d=await loadImage(url);if(d)images.push(d)}const data=await api.invoke('scout-enrich-listing',{capture,prior_analysis:c.analysis||{},images});if(generation!==O.generation||O.paused||O.stopped)return;if(data?.error)throw new Error(data.error);const a=applyEnrichment(c.analysis||{},data.result||{},capture,c),body={title:capture.title||c.title||null,asking_price:capture.askingPrice??c.asking_price??null,location:capture.location||c.location||null,condition:capture.condition||data.result?.condition_label||c.condition||null,seller_name:capture.sellerName||c.seller_name||null,thumbnail_url:capture.imageUrls?.[0]||c.thumbnail_url||null,deep_capture:capture,analysis:a,recommendation:a.recommendation||null,score:a.overall_score??null,resale_mid:a.resale_mid??null,expected_profit:a.expected_profit??null,expected_roi_percent:a.expected_roi_percent??null,rank_score:rank(a),updated_at:new Date().toISOString()};await api.update('scout_candidates',`id=eq.${c.id}`,body)}catch(error){if(generation===O.generation&&!O.paused&&!O.stopped&&error?.message!=='Cancelled'){const a={...(c.analysis||{}),scout_enriched:true,enrichment_error:error.message||String(error),authenticity_status:c.analysis?.authenticity_status||'uncertain'};if(['Sneakers','Audio','Watches','Fashion','Collectibles'].includes(category(c))){a.recommendation='verify_first';a.resale_low=null;a.resale_mid=null;a.resale_high=null;a.expected_profit=null;a.expected_roi_percent=null}await api.update('scout_candidates',`id=eq.${c.id}`,{analysis:a,recommendation:a.recommendation||c.recommendation,updated_at:new Date().toISOString()}).catch(()=>{})}}finally{O.enriching.delete(String(c.id));await liveSync();}}
function queueEnrichment(rows){if(O.paused||O.stopped)return;for(const c of rows.filter(c=>quickWorthwhile(c)&&!c.analysis?.scout_enriched)){if(O.enriching.has(String(c.id))||O.enrichQueue.some(x=>String(x.id)===String(c.id)))continue;O.enrichQueue.push(c)}pumpEnrichment()}
function pumpEnrichment(){if(O.paused||O.stopped)return;while(O.enrichWorkers<1&&O.enrichQueue.length){const c=O.enrichQueue.shift(),generation=O.generation;O.enrichWorkers++;enrichOne(c,generation).finally(()=>{O.enrichWorkers--;liveSync().catch(()=>{});pumpEnrichment()})}}
async function cancelBackgroundWork(){O.enrichQueue=[];O.active.clear();for(const id of [...O.enrichTabs])chrome.tabs.remove(id).catch(()=>{});O.enrichTabs.clear()}

async function fastScreen(){if(O.busy||O.paused||O.stopped)return;const stored=await activeScout();if(!stored?.sessionId)return;O.busy=true;const generation=O.generation;try{let rows=await loadRows(),rr=roundRows(rows);O.sessionId=stored.sessionId;O.total=rr.length;O.batchCount=Math.max(1,Math.ceil(rr.length/MAX_BATCH));renderLive(rows);queueEnrichment(rr);while(generation===O.generation&&!O.paused&&!O.stopped){rows=await loadRows();rr=roundRows(rows);const waiting=rr.filter(pending);if(!waiting.length)break;const processed=rr.filter(c=>rated(c)||failed(c)).length,chunk=waiting.slice(0,MAX_BATCH);O.batchNo=Math.min(O.batchCount,Math.floor(processed/MAX_BATCH)+1);O.batchStartedAt=Date.now();O.active.clear();chunk.forEach(c=>O.active.set(String(c.id),{started:Date.now()}));renderLive(rows);await Promise.allSettled(chunk.map(c=>rateOne(c,generation)));if(generation!==O.generation||O.paused||O.stopped)return;rows=await loadRows();rr=roundRows(rows);renderLive(rows);queueEnrichment(rr);await sleep(40)}if(generation!==O.generation||O.paused||O.stopped)return;rows=await loadRows();queueEnrichment(roundRows(rows));renderLive(rows)}finally{if(generation===O.generation){O.busy=false;O.active.clear();const rows=await loadRows().catch(()=>[]);renderLive(rows);refreshControlState()}}}

async function pauseScan(){if(O.paused||O.stopped)return;O.paused=true;O.generation+=1;O.busy=false;O.active.clear();await chrome.storage.local.set({[USER_PAUSE_KEY]:true});await cancelBackgroundWork();const rows=await loadRows();renderLive(rows);toast('Scout paused. Resume when you are ready.')}
async function resumeScan(){if(O.stopped)return toast('This Scout was stopped. Restart the current scan or start a new one.');if(!O.paused)return;O.paused=false;O.generation+=1;await chrome.storage.local.set({[USER_PAUSE_KEY]:false});const rows=await loadRows();renderLive(rows);toast('Scout resumed');fastScreen().catch(err=>toast(err.message))}
async function stopScan(){if(O.stopped)return;O.stopped=true;O.paused=false;O.generation+=1;O.busy=false;O.active.clear();await chrome.storage.local.set({[STOP_KEY]:true,[USER_PAUSE_KEY]:false});await cancelBackgroundWork();document.dispatchEvent(new CustomEvent('flippers:scout-hard-stop'));const rows=await loadRows();renderLive(rows);toast('Scout stopped completely. Completed results are saved.')}
async function restartRound(){const ctx=await sourceContext();if(!ctx.sourceAlive)return toast('The original Scout page is no longer open at the same URL.');O.generation+=1;O.stopped=false;O.paused=false;O.busy=false;O.active.clear();O.durations=[];O.batchNo=0;await cancelBackgroundWork();await chrome.storage.local.set({[STOP_KEY]:false,[USER_PAUSE_KEY]:false});const rows=await loadRows(),rr=roundRows(rows),reset={scan_status:'quick',analysis:{},recommendation:null,score:null,resale_mid:null,expected_profit:null,expected_roi_percent:null,rank_score:null,selected:false,deep_capture:{},updated_at:new Date().toISOString()};await Promise.all(rr.map(c=>api.update('scout_candidates',`id=eq.${c.id}`,reset).catch(()=>null)));renderLive(await loadRows());toast('Current Scout restarted from the first 5');fastScreen().catch(err=>toast(err.message))}
async function startNewScan(){const ctx=await sourceContext();if(!ctx.newCollection)return toast('Open a different marketplace results page first.');O.generation+=1;O.stopped=false;O.paused=false;O.active.clear();await cancelBackgroundWork();await chrome.storage.local.set({[STOP_KEY]:false,[USER_PAUSE_KEY]:false,[AUTO_NEW_KEY]:{requestedAt:Date.now(),target:normaliseUrl(ctx.activeTab.url)}});await chrome.storage.local.remove(ACTIVE_KEY);location.reload()}
async function autoStartNew(){const saved=(await chrome.storage.local.get(AUTO_NEW_KEY).catch(()=>({})))[AUTO_NEW_KEY];if(!saved)return;for(let i=0;i<70;i++){const button=$('#scanCurrent');if(button){await chrome.storage.local.remove(AUTO_NEW_KEY);button.click();return}await sleep(100)}await chrome.storage.local.remove(AUTO_NEW_KEY);toast('Could not start the new Scout automatically. Press Scan current page once.')}
async function removeCandidate(id){await api.remove('scout_candidates',`id=eq.${encodeURIComponent(id)}`).catch(()=>{});$(`.scout-candidate[data-candidate="${CSS.escape(String(id))}"]`)?.remove();const rows=await loadRows();renderLive(rows);toast('Removed from FlippersAI. Its marketplace rating stays saved.')}
async function syncNewSession(){const stored=await activeScout();if(!stored?.sessionId)return false;if(O.sessionId&&O.sessionId===stored.sessionId)return true;if(O.sessionId!==stored.sessionId){O.sessionId=stored.sessionId;O.generation+=1;O.stopped=false;O.paused=false;O.busy=false;O.active.clear();O.durations=[];O.batchNo=0;await cancelBackgroundWork();await chrome.storage.local.set({[STOP_KEY]:false,[USER_PAUSE_KEY]:false});return true}return true}
function scheduleStatus(){clearTimeout(O.timer);O.timer=setTimeout(async()=>{ensureUi();const rows=await loadRows().catch(()=>[]);renderLive(rows);refreshControlState()},120)}

document.addEventListener('click',e=>{if(e.target.closest?.('#v080Stop')){e.preventDefault();e.stopImmediatePropagation();stopScan().catch(err=>toast(err.message));return}if(e.target.closest?.('#v080Pause')){e.preventDefault();e.stopImmediatePropagation();(O.paused?resumeScan():pauseScan()).catch(err=>toast(err.message));return}if(e.target.closest?.('#v080Restart')){e.preventDefault();e.stopImmediatePropagation();restartRound().catch(err=>toast(err.message));return}if(e.target.closest?.('#v080New')){e.preventDefault();e.stopImmediatePropagation();startNewScan().catch(err=>toast(err.message));return}const remove=e.target.closest?.('[data-v076-remove]');if(remove){e.preventDefault();e.stopImmediatePropagation();removeCandidate(remove.dataset.v076Remove).catch(err=>toast(err.message))}},true)
document.addEventListener('flippers:scout-rendered',async()=>{await syncNewSession();ensureUi();const rows=await loadRows();renderLive(rows);if(!O.paused&&!O.stopped)fastScreen().catch(err=>toast(err.message))})
document.addEventListener('flippers:candidate-updated',scheduleStatus)
new MutationObserver(ms=>{if(ms.every(m=>m.target.closest?.('#v080RunBar,#v080Loading,.scout-summary,.scout-insight')))return;scheduleStatus()}).observe(document.getElementById('app'),{childList:true,subtree:true,attributes:true,attributeFilter:['class']})

;(async()=>{const stored=await chrome.storage.local.get([USER_PAUSE_KEY,STOP_KEY]).catch(()=>({}));O.paused=Boolean(stored[USER_PAUSE_KEY]);O.stopped=Boolean(stored[STOP_KEY]);const active=await activeScout();O.sessionId=active?.sessionId||null;ensureUi();clearInterval(O.contextTimer);O.contextTimer=setInterval(()=>{removeDuplicateControls();refreshControlState().catch(()=>{})},800);clearInterval(O.heartbeat);O.heartbeat=setInterval(async()=>{if(!O.busy&&!O.active.size)return;const rows=await loadRows().catch(()=>[]);renderLive(rows)},1000);autoStartNew().catch(()=>{});if(active?.sessionId){const rows=await loadRows();renderLive(rows);queueEnrichment(roundRows(rows));if(!O.paused&&!O.stopped)fastScreen().catch(err=>toast(err.message))}})()
